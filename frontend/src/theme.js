import { createTheme, colors } from '@mui/material'

const myTheme = createTheme({
    palette: {
        primary: {
            main: '#077088',
        },
        secondary: {
            main: '#f28db2',
        },
        info: {
            main: '#fff'
        },
        text: {
            primary: '#fff',
            secondary: '#75c1d7',
        }
    }
})

export default myTheme;